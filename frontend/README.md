# Frontend consolidated

The only Expo app is now at the repository root. Run `npm install` and `npx expo start --clear` there. Routes live in `app/`; shared UI and services live in `src/`. Do not run Expo from this folder.
